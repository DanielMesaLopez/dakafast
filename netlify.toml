# ✅ netlify.toml limpio y funcional para Expo Web

[build]
  command = "npm run build:web"
  publish = "dist"

[build.environment]
  EXPO_USE_WEB = "true"
  NODE_VERSION = "18"

# 🚀 Redirección para SPA (Single Page App)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
